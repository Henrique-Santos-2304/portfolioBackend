import styled from 'styled-components';

const SearchButton = styled.button`
  padding: 0 10px;
  line-height: normal;
  color: #f8f8f8;
  &:focus {
    outline: 0;
  }
`;

export default SearchButton;
